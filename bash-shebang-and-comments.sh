#shebang :   #!/bin/bash, is the directive , the default shell 
#the shebang indicates the direction of path to the bash shell
which bash 
which -a bash 
ls -li
# ls -ltr : To sort the file names in the last modification time in reverse order
